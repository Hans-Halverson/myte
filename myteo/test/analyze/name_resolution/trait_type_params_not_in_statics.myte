module test

trait Foo<T> {
  // Trait type parameters can appear in non-static methods
  fun foo(x: T) {}

  // Trait type parameters cannot appear in static methods
  static fun bar(x: T) {}
}

fun main() {}