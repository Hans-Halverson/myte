module test

fun ints() {
  // Numeric operations
  1 + 2;
  1 - 2;
  1 * 2;
  1 / 2;
  1 % 2;

  // Bitwise operations
  1 & 2;
  1 | 2;
  1 ^ 2;
  1 << 2;
  1 >> 2;
  1 >>> 2;

  // Comparisons
  1 == 2;
  1 != 2;
  1 < 2;
  1 <= 2;
  1 > 2;
  1 >= 2;
}

fun bytes(x: byte, y: byte) {
  x + y;
  x - y;
  x * y;
  x / y;
  x % y;

  // Bitwise operations
  x & y;
  x | y;
  x ^ y;
  x << y;
  x >> y;
  x >>> y;

  x == y;
  x != y;
  x < y;
  x <= y;
  x > y;
  x >= y;
}

fun longs(x: long, y: long) {
  x + y;
  x - y;
  x * y;
  x / y;
  x % y;

  // Bitwise operations
  x & y;
  x | y;
  x ^ y;
  x << y;
  x >> y;
  x >>> y;

  x == y;
  x != y;
  x < y;
  x <= y;
  x > y;
  x >= y;
}

fun main() {}