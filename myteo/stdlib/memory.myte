module std.memory

import std.int.Int

builtin type Array<T>

methods Array<T> {
  builtin static fun new<T>(count: Int): Array<T>

  builtin static fun copy<T>(dest: Array<T>, src: Array<T>, count: Int)
}