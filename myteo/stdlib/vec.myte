module std.vec

import std.array
import std.array.Array
import std.int.Int

type Vec<T> {
  data: Array<T>,
  size: Int,
  capacity: Int,
}

fun new<T>(capacity: Int): Vec<T> {
  return Vec {
    data: array.new(capacity),
    size: 0,
    capacity,
  };
}