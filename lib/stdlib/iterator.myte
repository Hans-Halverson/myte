module std.iterator

import std.option.Option

pub trait Iterator<T> {
  extends Iterable<T>

  fun next(): Option<T>

  override fun toIterator(): This = this
}

pub trait Iterable<T> {
  fun toIterator(): Iterator<T>
}