  .global _main

  .text
_main:
  leaq test.sextByteToInt(%rip), %rdi
  call test.use$fn$
  leaq test.sextByteToLong(%rip), %rdi
  call test.use$fn$
  leaq test.sextIntToLong(%rip), %rdi
  call test.use$fn$
  movl $0, %eax
  ret
test.sextByteToInt:
  movsx %dil, %eax
  ret
test.sextByteToLong:
  movsx %dil, %rax
  ret
test.sextIntToLong:
  movsx %edi, %rax
  ret
test.use$fn$:
  ret