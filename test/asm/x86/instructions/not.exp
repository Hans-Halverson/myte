  .global _main

  .text
_main:
  leaq test.notByteReg(%rip), %rdi
  call test.use$fn$
  leaq test.notIntReg(%rip), %rdi
  call test.use$fn$
  leaq test.notLongReg(%rip), %rdi
  call test.use$fn$
  movl $0, %eax
  ret
test.notByteReg:
  notb %dil
  movl %edi, %eax
  ret
test.notIntReg:
  notl %edi
  movl %edi, %eax
  ret
test.notLongReg:
  notq %rdi
  movq %rdi, %rax
  ret
test.use$fn$:
  movq %rdi, %rax
  ret