  .global _main

  .bss
  .balign 8
test.s:
  .skip 8

  .data
.S0:
  .ascii "foo"
.S1:
  .ascii "foobar"

  .text
_myte_init:
  str x30, [sp, #-16]!
  movz w0, #16
  bl mytec_alloc
  adrp x1, .S0
  add x1, x1, :lo12:.S0
  str x1, [x0, #8]
  movz w1, #3
  str w1, [x0]
  movz w1, #3
  str w1, [x0, #4]
  adrp x1, test.s
  str x0, [x1, :lo12:test.s]
  ldr x30, [sp], #16
  ret
_main:
  str x30, [sp, #-16]!
  adrp x0, test.s
  ldr x0, [x0, :lo12:test.s]
  bl test.use$String$
  mov w0, wzr
  ldr x30, [sp], #16
  ret
test.test:
  str x30, [sp, #-16]!
  movz w0, #16
  bl mytec_alloc
  adrp x1, .S1
  add x1, x1, :lo12:.S1
  str x1, [x0, #8]
  movz w1, #6
  str w1, [x0]
  movz w1, #6
  str w1, [x0, #4]
  ldr x30, [sp], #16
  ret
test.use$String$:
  ret