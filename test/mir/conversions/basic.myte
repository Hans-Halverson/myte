module test

fun testConvertByte(x: Byte) {
  x.toInt();
  x.toLong();
}

fun testConvertInt(x: Int) {
  x.toByte();
  x.toLong();
}

fun testConvertLong(x: Long) {
  x.toByte();
  x.toInt();
}

fun use<T>(x: T) {}

fun main() {
  use(testConvertByte);
  use(testConvertInt);
  use(testConvertLong);
}