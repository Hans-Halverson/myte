module test

fun use<T>(x: T) {}

val x: Int = 1

fun test1(y: Int, z: Unit) {
  x + 2 * y;
  z;
}

fun test2() {
  val x: Int = x;
  val y: Int = 4 * 34;

  use(x);
  use(y);
}

fun main() {
  use(test1);
  use(test2);
}