module test

fun use<T>(x: T) {}

fun empty() {}

fun emptyParams(x: Int, y: Bool) {}

fun shorthand(x: Int): Int = 1

fun withBody(x: Int, y: Bool, z: Bool) {
  use(10);
  val zz: Bool = y && z;
}

fun main() {
  use(true);
}