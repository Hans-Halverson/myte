module test

fun bools(x: Bool) {
  !x;
}

fun ints(x: Int) {
  -x;
  !x;
}

fun bytes(x: Byte) {
  -x;
  !x;
}

fun longs(x: Long) {
  -x;
  !x;
}

fun use<T>(x: T) {}

fun main() {
  use(bools);
  use(ints);
  use(bytes);
  use(longs);
}