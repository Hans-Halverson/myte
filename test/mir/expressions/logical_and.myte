module test

fun testConstants1(): Bool = true && true

fun testConstants2(): Bool = true && false

fun testConstants3(): Bool = false && true

fun testConstants4(): Bool = false && false

fun testConstant1(x: Bool): Bool = x && true

fun testConstant2(x: Bool): Bool = x && false

fun testConstant3(x: Bool): Bool = true && x

fun testConstant4(x: Bool): Bool = false && x

fun testVariables(x: Bool, y: Bool): Bool = x && y

fun use<T>(x: T) {}

fun main() {
  use(testConstants1);
  use(testConstants2);
  use(testConstants3);
  use(testConstants4);
  use(testConstant1);
  use(testConstant2);
  use(testConstant3);
  use(testConstant4);
  use(testVariables);
}