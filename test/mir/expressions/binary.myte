module test

fun ints() {
  // Numeric operations
  1 + 2;
  1 - 2;
  1 * 2;
  1 / 2;
  1 % 2;

  // Bitwise operations
  1 & 2;
  1 | 2;
  1 ^ 2;
  1 << 2;
  1 >> 2;
  1 >>> 2;

  // Comparisons
  1 < 2;
  1 <= 2;
  1 > 2;
  1 >= 2;
}

fun bytes(x: Byte, y: Byte) {
  x + y;
  x - y;
  x * y;
  x / y;
  x % y;

  // Bitwise operations
  x & y;
  x | y;
  x ^ y;
  x << y;
  x >> y;
  x >>> y;

  x < y;
  x <= y;
  x > y;
  x >= y;
}

fun longs(x: Long, y: Long) {
  x + y;
  x - y;
  x * y;
  x / y;
  x % y;

  // Bitwise operations
  x & y;
  x | y;
  x ^ y;
  x << y;
  x >> y;
  x >>> y;

  x < y;
  x <= y;
  x > y;
  x >= y;
}

fun main() {}